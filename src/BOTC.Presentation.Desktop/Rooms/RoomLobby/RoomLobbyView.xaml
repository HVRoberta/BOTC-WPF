<UserControl x:Class="BOTC.Presentation.Desktop.Rooms.RoomLobby.RoomLobbyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="420">
    <StackPanel>
        <TextBlock FontSize="22" FontWeight="SemiBold" Text="Room Lobby" />
        <TextBlock Margin="0,12,0,0" Text="This is a placeholder lobby target." />

        <TextBlock Margin="0,16,0,0" Text="Room ID" FontWeight="SemiBold" />
        <TextBlock Text="{Binding RoomId}" />

        <TextBlock Margin="0,12,0,0" Text="Room Code" FontWeight="SemiBold" />
        <TextBlock Text="{Binding RoomCode}" />

        <TextBlock Margin="0,12,0,0" Text="Created (UTC)" FontWeight="SemiBold" />
        <TextBlock Text="{Binding CreatedAtUtc}" />

        <Button Margin="0,20,0,0"
                Width="170"
                Height="34"
                HorizontalAlignment="Left"
                Command="{Binding BackToCreateRoomCommand}"
                Content="Create another room" />
    </StackPanel>
</UserControl>

