<UserControl x:Class="BOTC.Presentation.Desktop.Features.Profiles.UserProfileSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:BOTC.Presentation.Desktop.Features.Profiles"
             mc:Ignorable="d"
             d:DesignHeight="800" 
             d:DesignWidth="1200">
    <Grid>
        <!-- Background -->
        <Grid.Background>
            <ImageBrush ImageSource="/BOTC.Presentation.Desktop;component/Shared/Assets/editor-background.png" 
                        Stretch="UniformToFill"/>
        </Grid.Background>
        
        <!-- Dark overlay -->
        <Rectangle Fill="#660B0F1A"/>
        
        <!-- Center panel -->
        <Border Width="760"
                MaxWidth="760"
                Padding="28"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                CornerRadius="18"
                Background="#B8141824"
                BorderBrush="#C89B4A"
                BorderThickness="2">
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  MaxWidth="900">
            
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Title -->
                <TextBlock Grid.Row="0"
                           Text="WHO IS PLAYING?"
                           HorizontalAlignment="Center"
                           FontSize="34"
                           FontWeight="Bold"
                           Foreground="#F1C36A"
                           Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1"
                           Text="Select a saved profile or create a new one."
                           HorizontalAlignment="Center"
                           FontSize="18"
                           Foreground="#F2E5C8"
                           Margin="0,0,0,8"/>
                
                <!-- Profiles -->
                <ListBox Grid.Row="3"
                         ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,14"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="RootBorder"
                                                Background="#66151A24"
                                                BorderBrush="#55C89B4A"
                                                BorderThickness="1"
                                                CornerRadius="14"
                                                Padding="14">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="#F1C36A"/>
                                                <Setter TargetName="RootBorder" Property="BorderThickness" Value="2"/>
                                                <Setter TargetName="RootBorder" Property="Background" Value="#88301E12"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="RootBorder" Property="BorderBrush" Value="#D8B06A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="74"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>

                                <!-- Avatar -->
                                <Grid Width="58" Height="58" VerticalAlignment="Center">
                                    <Ellipse Fill="#2A2F3A"
                                             Stroke="#C89B4A"
                                             StrokeThickness="2"/>
                                    <TextBlock Text="👤"
                                               FontSize="24"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Grid>

                                <!-- Text -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayName}"
                                               FontSize="22"
                                               FontWeight="SemiBold"
                                               Foreground="#F6E7C8"/>
                                    <TextBlock Text="{Binding Subtitle}"
                                               FontSize="15"
                                               Foreground="#D4B98A"/>
                                </StackPanel>

                                <!-- Action -->
                                <Button Grid.Column="2"
                                        Width="52"
                                        Height="52"
                                        Command="{Binding DataContext.SelectProfileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent"
                                        BorderBrush="#A27A3A"
                                        BorderThickness="1.5"
                                        Foreground="#F1C36A"
                                        FontSize="24"
                                        Content="›"
                                        Cursor="Hand"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                
                <!-- Create button -->
                <Button Grid.Row="5"
                        Height="58"
                        Command="{Binding CreateProfileCommand}"
                        Background="#4E2A18"
                        BorderBrush="#C89B4A"
                        BorderThickness="2"
                        Foreground="#F7E6BF"
                        FontSize="22"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Content="Create New Profile"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
